apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: remote-connect-mcp-java-center
  namespace: remote-connect-mcp
  labels:
    app.kubernetes.io/name: remote-connect-mcp-java-center
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: mnnu-gateway
      namespace: gateway-system
      sectionName: https
  hostnames:
    # Keep the established gateway alias for MCP clients.  Production overlays
    # may reuse the existing remote-connect-mcp-* records without creating a
    # second DNS name just because the implementation is Java.
    - remote-connect-mcp-gateway.example.invalid
    - remote-connect-mcp-agent.example.invalid
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: remote-connect-mcp-java-center
          port: 8080
      timeouts:
        request: 0s
        backendRequest: 0s
