apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: remote-connect-mcp-center-state
  namespace: remote-connect-mcp
  labels:
    app.kubernetes.io/name: remote-connect-mcp-center
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
