apiVersion: v1
kind: Service
metadata:
  name: remote-connect-mcp-center
  namespace: remote-connect-mcp
  labels:
    app.kubernetes.io/name: remote-connect-mcp-center
spec:
  selector:
    app.kubernetes.io/name: remote-connect-mcp-center
  ports:
    - name: http
      port: 8080
      targetPort: http
